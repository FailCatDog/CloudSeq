<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.guet.feishu.mapper.ProjectTaskMapper">

    <resultMap id="BaseResultMap" type="cn.guet.feishu.entity.ProjectTask">
        <id column="id" property="id"/>
        <result column="task_id" property="taskId"/>
        <result column="project_id" property="projectId"/>
        <result column="group_id" property="groupId"/>
        <result column="task_title" property="taskTitle"/>
        <result column="task_description" property="taskDescription"/>
        <result column="task_code" property="taskCode"/>
        <result column="priority" property="priority"/>
        <result column="status" property="status"/>
        <result column="progress" property="progress"/>
        <result column="parent_task_id" property="parentTaskId"/>
        <result column="creator_id" property="creatorId"/>
        <result column="start_time" property="startTime"/>
        <result column="end_time" property="endTime"/>
        <result column="estimated_hours" property="estimatedHours"/>
        <result column="actual_hours" property="actualHours"/>
        <result column="tags" property="tags"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
    </resultMap>

    <insert id="insert" parameterType="cn.guet.feishu.entity.ProjectTask">
        INSERT INTO project_task (
            task_id, project_id, group_id, task_title, task_description,
            task_code, priority, status, progress, parent_task_id,
            creator_id, start_time, end_time, estimated_hours, actual_hours, tags
        ) VALUES (
            #{taskId}, #{projectId}, #{groupId}, #{taskTitle}, #{taskDescription},
            #{taskCode}, #{priority}, #{status}, #{progress}, #{parentTaskId},
            #{creatorId}, #{startTime}, #{endTime}, #{estimatedHours}, #{actualHours}, #{tags}
        )
    </insert>

    <update id="updateByTaskId" parameterType="cn.guet.feishu.entity.ProjectTask">
        UPDATE project_task
        <set>
            <if test="taskTitle != null">task_title = #{taskTitle},</if>
            <if test="taskDescription != null">task_description = #{taskDescription},</if>
            <if test="priority != null">priority = #{priority},</if>
            <if test="status != null">status = #{status},</if>
            <if test="progress != null">progress = #{progress},</if>
            <if test="startTime != null">start_time = #{startTime},</if>
            <if test="endTime != null">end_time = #{endTime},</if>
            <if test="estimatedHours != null">estimated_hours = #{estimatedHours},</if>
            <if test="actualHours != null">actual_hours = #{actualHours},</if>
            <if test="tags != null">tags = #{tags},</if>
        </set>
        WHERE task_id = #{taskId}
    </update>

    <delete id="deleteByTaskId">
        DELETE FROM project_task WHERE task_id = #{taskId}
    </delete>

    <select id="selectByTaskId" resultMap="BaseResultMap">
        SELECT * FROM project_task WHERE task_id = #{taskId}
    </select>

    <select id="selectByProjectId" resultMap="BaseResultMap">
        SELECT * FROM project_task 
        WHERE project_id = #{projectId}
        ORDER BY priority ASC, end_time ASC
    </select>

    <select id="selectByGroupId" resultMap="BaseResultMap">
        SELECT * FROM project_task 
        WHERE group_id = #{groupId}
        ORDER BY priority ASC, end_time ASC
    </select>

    <select id="selectByParentTaskId" resultMap="BaseResultMap">
        SELECT * FROM project_task 
        WHERE parent_task_id = #{parentTaskId}
        ORDER BY create_time ASC
    </select>

    <select id="selectByCreatorId" resultMap="BaseResultMap">
        SELECT * FROM project_task 
        WHERE creator_id = #{creatorId}
        ORDER BY create_time DESC
    </select>

    <select id="selectMaxTaskCode" resultType="string">
        SELECT task_code FROM project_task
        WHERE project_id = #{projectId}
        ORDER BY task_code DESC
        LIMIT 1
    </select>

    <select id="selectByCondition" resultMap="BaseResultMap">
        SELECT * FROM project_task
        <where>
            <if test="projectId != null">AND project_id = #{projectId}</if>
            <if test="groupId != null">AND group_id = #{groupId}</if>
            <if test="status != null">AND status = #{status}</if>
            <if test="priority != null">AND priority = #{priority}</if>
        </where>
        ORDER BY priority ASC, end_time ASC
    </select>

</mapper>

