---
description: 
alwaysApply: true
---

# 强制编码规范

约束生成的前端代码保持体量小、与接口及表单结构一致、拒绝无依据的默认值。按需与项目其他说明合并使用。

**权衡：** 这些规则偏向「数据忠实、改动面小」，而不是堆「防御式」便利代码。对极小的单行修改可自行把握松紧。**第 5 节注释范围不适用放宽，一律严格执行。**

## 1. 极简优先

**能少则少。拒绝无意义的包裹层。**

在动手写代码前，守住结构边界：
- 拒绝过度设计、深层套娃、冗余分层、性价比低的抽象。
- 禁止把仅一两行逻辑、纯转发调用、简单取值赋值的片段抽成函数——必须内联。
- 工具函数、自定义 Hook、公共方法仅在「多处复用」**或**逻辑确实冗长复杂时引入；禁止为「好看」而拆分简单逻辑。
- 若值直接来自另一变量，禁止再塞一层无意义的中间变量，应直接赋值。
- 禁止随意新建文件；除非用户明确要求新建文件，否则**必须先询问**。

自问：「资深同事会不会觉得这层封装是噪音？」若是，删掉封装并内联。

## 2. 尊重原始数据

**接口与表单长什么样，代码就按什么样用。禁止无差别兜底。**

处理接口返回、入参、表单与状态时：
- 禁止在没有明确业务理由、也没有具体故障场景时，强行填充兜底值或「凑齐」默认值。
- 对变量、对象字段、接口参数、状态字段：
  - 业务未要求时，**禁止**随手写入 `''`、`null`、`[]`、`{}`、`0` 等哨兵默认值。
  - **禁止**滥用 `||`、`??`、三元运算符做「以防万一」式容错。
  - **禁止**给可选、非必传字段钉死固定默认值，以免污染真实契约。
- 严格遵循数据原始结构：有值用原值，无值保持缺失/可选，**禁止**擅自补字段、造字段。
- 兜底逻辑只写在**已确认会报错或边界异常已知**的场景；普通业务流程**禁止**习惯性加一层额外防御。

验收标准：若删掉你加的「贴心」默认值，代码是否仍与后端真实契约一致？若否，说明过度归一化了。

## 3. 直白逻辑与类型

**路径要直。不要表演式代码。**

编写分支、取值、格式化与条件渲染时：
- 使用最简单可读的形式，禁止迂回炫技。
- 禁止冗余空判断、永远无法命中的 `else`、空转一圈的 `try/catch`、无实质作用的拦截逻辑。
- TypeScript 类型须贴合业务：禁止随意加宽、滥用 `any`、堆多余可选修饰、在泛型上乱加默认类型兜底。
- 不生成重复片段、废弃占位变量、不交付的「TODO」脚手架；注释粒度见**第 5 节**。

## 4. 手术式交付

**只交付被请求的那一块。别动无关默认值习惯。**

交付与修改时：
- 输出干净、精简：无冗余字段、无擅自添加的默认值。
- 若无特殊说明，只实现**核心需求**；禁止主动扩展额外容错、兼容、兜底逻辑。
- 修改现有代码时，只动与需求直接相关的部分；**禁止**大范围改写无关字段形状或项目里既有的默认赋值习惯。

## 5. 注释范围（**严格执行**）

**只允许方法级注释；其余一律禁止，无例外、不凭「可读性」自行放宽。**

- **允许（仅此一类）**：写在**具名函数 / 方法 / 组件内具名导出函数**（及等价「一块独立可调用逻辑」的顶层声明，如 `function foo` / `const foo = () => {}` 中 **`foo` 紧上方**）上的简短注释——仅用于说明入参出参约定、副作用、与命名无法自解释的业务约束；宜一行到数行，禁止长篇文档。
- **禁止（凡出现即视为违规，须删除或改为命名）**：
  - `ref` / `reactive` / `computed` / 模块级常量 / 类型别名 / `defineProps` 某条属性等**上方的块注释或行尾注释**（说明「这个变量干什么」一律不允许）；
  - 方法体内的分段注释、逐步说明、行尾解释显而易见代码的注释；
  - 模板里为「说明 UI」或结构而写的注释；
  - 用注释代替命名（例如变量名含糊靠注释补语义）——应改命名而非加注释。
- **协作要求**：生成或修改代码时默认**不写**注释，除非满足上一款「允许」且确有必要；拿不准时**不写**。

## 6. Vue 父子组件通信

**单向数据流：props 入、emit 出；父编排业务与数据，子聚焦 UI 与局部交互。**

- **父组件**：编排整块界面与数据从哪来、往哪去、子块之间如何配合；持有与页面、业务强相关的状态（列表、当前选中、待落库表单等）；在合宜时机调用接口完成拉取、保存、删除等；在多个子域之间协调（联动清空、是否允许保存等）。通过 **props** 下发子组件所需的只读输入与展示数据；通过监听子组件 **emit** 更新自身状态、继续调接口或切换路由。一般不承担细碎纯 UI 实现细节。
- **子组件**：可复用或可替换的一块 UI 与局部交互；依 **props** 渲染；仅影响本块自身的交互可在内部处理（展开收起、焦点等）。用户操作具有业务含义时（选中一项、提交表单、删除一行等），须通过 **emit** 把**结构化事实**交给父组件；**不在子组件内独自维护整条业务链的最终事实来源**。`v-model`、`provide/inject`、插槽等可作为附属手段，**默认仍以 props + emit 为规范底线**。

**逻辑边界**

- **父**：定义与管理状态（`ref` / `reactive`）；发起 API、副作用与异步；承载修改数据的业务方法；多子组件协调；响应子组件事件并更新数据；向子传递必要 props；按需使用 `computed` / `watch` 派生或监听。
- **子**：`defineProps` 接收所需数据；`defineEmits` 声明事件并在用户交互时触发；仅 **与 UI 相关的临时状态**（如下拉是否展开、输入框本地草稿）；依 props 渲染，可展示加载或错误占位；**不得**直接修改 props；**不得**在子组件内发起 API 请求；**不得**堆积与父职责重复的复杂业务逻辑。

---

**规范生效时可观察为：** diff 面小、对象形态与接口返回一致且无编造字段；只有出现真实复用或真实复杂度时，才出现新的抽象或新文件；**除具名函数/方法紧上方的简短说明外无注释**，无行内噪音、无变量/属性旁说明块；父子边界清晰时，业务状态与接口调用集中在父、子以 props/emit 与父对齐。
