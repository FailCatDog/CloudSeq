<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.guet.feishu.mapper.DocumentFolderMapper">

    <resultMap id="DocumentFolderResultMap" type="cn.guet.feishu.entity.DocumentFolder">
        <id property="id" column="id"/>
        <result property="folderId" column="folder_id"/>
        <result property="groupId" column="group_id"/>
        <result property="folderName" column="folder_name"/>
        <result property="parentFolderId" column="parent_folder_id"/>
        <result property="creatorId" column="creator_id"/>
        <result property="status" column="status"/>
        <result property="createTime" column="create_time"/>
        <result property="updateTime" column="update_time"/>
    </resultMap>

    <insert id="insert" parameterType="cn.guet.feishu.entity.DocumentFolder">
        INSERT INTO document_folder (folder_id, group_id, folder_name, parent_folder_id, creator_id, status)
        VALUES (#{folderId}, #{groupId}, #{folderName}, #{parentFolderId}, #{creatorId}, #{status})
    </insert>

    <update id="update" parameterType="cn.guet.feishu.entity.DocumentFolder">
        UPDATE document_folder
        <set>
            <if test="folderName != null">folder_name = #{folderName},</if>
            <if test="status != null">status = #{status},</if>
        </set>
        WHERE folder_id = #{folderId}
    </update>

    <delete id="deleteById">
        DELETE FROM document_folder WHERE folder_id = #{folderId}
    </delete>

    <select id="findById" resultMap="DocumentFolderResultMap">
        SELECT * FROM document_folder WHERE folder_id = #{folderId}
    </select>

    <select id="findByGroupId" resultMap="DocumentFolderResultMap">
        SELECT * FROM document_folder 
        WHERE group_id = #{groupId} AND status = 1
        ORDER BY create_time DESC
    </select>

    <select id="findByParentFolderId" resultMap="DocumentFolderResultMap">
        SELECT * FROM document_folder 
        WHERE parent_folder_id = #{parentFolderId} AND status = 1
        ORDER BY create_time DESC
    </select>

    <select id="countByParentFolderId" resultType="int">
        SELECT COUNT(*) FROM document_folder 
        WHERE parent_folder_id = #{parentFolderId} AND status = 1
    </select>

</mapper>

